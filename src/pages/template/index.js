const { response } = require("express");

(function(w, d, qns){
  w.addEventListener('load', () => {
    
    /* Código JS aqui ... */

    // Modelo de requisição: 
    // try {
      // Modelo de requisição do tipo GET
      // const response = await qns.api.get('url');

      // Modelo de requisição do tipo POST
      // const response = await qns.api.post('url', {
         // json
      //});
      
      // const data = response.data;

      // tratamento da resposta

    // } catch (error) {
      // err.response.data to get server message
      // console.log(error);
    // }

    // Criação de elementos dinamicos JS
    // Tratar array de objetos
    
    //let item = d.createElement('li')
    //item.setAttribute("data-id", lista[n]._id)
    //d.appendChild('ul')

  })
})(window, document, window.qns = window.qns || {});